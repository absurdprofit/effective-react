{
  "name": "root",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "lerna run build",
    "dev": "lerna run dev --parallel",
    "publish": "lerna run publish --no-private --parallel",
    "publish:dry": "lerna run publish:dry --no-private --parallel",
    "test": "lerna run test --no-private --parallel",
    "lint": "lerna run lint --parallel"
  },
  "workspaces": [
    "packages/*",
    "example"
  ],
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "eslint": "^9.39.1",
    "eslint-plugin-react-compiler": "^19.1.0-rc.2",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.4.24",
    "jsr": "^0.13.5",
    "lerna": "^9.0.3",
    "typescript-eslint": "^8.46.4",
    "vite": "^7.3.0",
    "vitest": "^4.0.16"
  },
  "os": [
    "darwin",
    "linux",
    "win32"
  ],
  "engines": {
    "node": "^22.14.0"
  }
}
