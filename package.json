{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "dev": "lerna run dev --parallel",
    "publish": "lerna run publish --parallel",
    "publish:dry": "lerna run publish:dry --parallel"
  },
  "workspaces": [
    "packages/*",
    "example"
  ],
  "devDependencies": {
    "jsr": "^0.13.5",
    "lerna": "^9.0.3",
    "vite": "^7.3.0",
    "vitest": "^4.0.16"
  }
}
