{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "dev": "lerna run dev --parallel"
  },
  "workspaces": [
    "packages/*",
    "example"
  ],
  "devDependencies": {
    "lerna": "^9.0.3",
    "vite": "^7.3.0",
    "vitest": "^4.0.16"
  }
}
